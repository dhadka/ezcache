name: 'ezcache'
description: ''
inputs:
  type:
    description: 'The type of cache to create'
    required: true
runs:
  using: 'node12'
  main: 'dist/restore/index.js'
  post: 'dist/save/index.js'
  post-if: 'success()'